This is how i solved task 8 by digging in to the string class methods the other day :
[10:43 AM]
int [] numbers={10,15,12,9,22,88,22,9};

        int smallest = Arrays.stream(numbers).min().getAsInt();
            System.out.println("the smallest number in the array is:  " +smallest );

             System.out.println();

            int largest = Arrays.stream(numbers).max().getAsInt();
        System.out.println("the largest number in the Array is: " +largest);